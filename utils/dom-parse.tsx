/** * Created by WebStorm. * User: chrischen * Date: 2020/9/19 * Time: 6:38 下午 */import React from 'react';// 真实DOM => 字符串export function htmlParseStr(el: HTMLElement): string {    let temp = document.createElement('div') as HTMLDivElement;    temp.appendChild(el);    return temp.innerHTML;}// 字符串 => ReactDOMexport function strParseVirtualDOM(strHtml: string): any {    return <div dangerouslySetInnerHTML={ { __html: strHtml } }/>;}// 真实DOM 转 ReactDOMexport function htmlParseVirtualDOM(el: HTMLElement): any {    let strHtml = htmlParseStr(el);    return strParseVirtualDOM(strHtml);}
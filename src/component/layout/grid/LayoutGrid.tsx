/** * Created by WebStorm. * User: MacBook * Date: 2020/12/24 * Time: 5:35 下午 */import React, { Component } from 'react';import { IComponentProps } from '@interface/common/component';import { Col, Row } from 'antd';export default class LayoutGrid extends Component<IComponentProps, any> {    constructor(props) {        super(props);    }    render() {        return <>            <Row>                { this.props.elChildren.map((child, index) => {                    let span = child.getAttribute('data-grid') ?? (24 / this.props.elChildren.length);                    return <Col span={ span } key={ index }>                        <div ref={ el => {                            if (el) {                                el.append(child);                            }                        } }/>                    </Col>;                }) }            </Row>        </>;    }}
/** * Created by WebStorm. * User: MacBook * Date: 2020/12/9 * Time: 5:52 下午 */import React, { Component } from 'react';import { IComponentProps } from '@interface/common/component';import { Tooltip } from 'antd';import { strParseVirtualDOM } from '@utils/parser-dom';import $ from 'jquery';export default class TipsText extends Component<IComponentProps, any> {    render() {        setTimeout(() => {            this.props.el.childNodes[0].textContent = '';        });        return <Tooltip title={ this.props.dataset.content } color={ this.props.dataset.color }>            <span>{ strParseVirtualDOM(this.props.beforeElement.innerHTML) }</span>        </Tooltip>;    }}
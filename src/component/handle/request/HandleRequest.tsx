/** * Created by WebStorm. * User: MacBook * Date: 2020/10/26 * Time: 3:30 下午 */import React from 'react';import { IComponentProps } from '@interface/common/component';import { jsonp } from '@utils/request/request';import { message } from 'antd';import axios from 'axios';export default class HandleRequest extends React.Component<IComponentProps, any> {    constructor(props) {        super(props);        this.props.el.onclick = e => this.handleRequest(e);    }    async handleRequest(e) {        try {            if (this.props.dataset.url) {                let { status, msg } = await jsonp(this.props.dataset.url);                message.success(status ? '操作成功' : '操作失败');            }        } catch (error) {            message.success(error.message);        }    }    render() {        return <></>;    }}
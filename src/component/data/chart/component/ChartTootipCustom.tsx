/** * Created by WebStorm. * User: MacBook * Date: 2021/1/12 * Time: 11:01 上午 */import { EyeFilled } from '@ant-design/icons';import React from 'react';import style from '../DataChart.scss';interface IChartTootipCustom {    config: {        title?: string        items?: Array<any>        [key: string]: any    }    suffix?: string}export function ChartTootipCustom(props: IChartTootipCustom) {    let { title, items } = props.config;    items = items || [];    console.log(props);    if (items.length > 1) {        console.log('分组');    }    if (items.length === 1) {    }    let { suffix } = props;    return <div style={ { padding: '10px 20px' } }>        <p style={ { marginLeft: -20, fontWeight: 'bold' } }>{ title }</p>        { items.map(item =>            <div className={ style.line }> { item.name } : { item.value }{ suffix ?? '' }                <EyeFilled className={ style.icon } style={ { color: item.color } }/>            </div>)        }    </div>;}
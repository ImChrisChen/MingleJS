import './src/App'import { getComponent } from "./utils/relationMap";console.log('哈哈哈哈hahah谁😂');export class Main {    // private readonly el: HTMLElement;    constructor() {        this.getAttributes();    }    getAttributes() {        let elements = document.querySelectorAll(`[data-fn]`);        elements.forEach(async (element, index) => {            let container = document.createElement('div');            let id = `MingleModule-${ index }`;            element.setAttribute('id', id)            container.setAttribute('data-for', id)            element.after(container);            let moduleName = element.getAttribute('data-fn') ?? "";            let esModule = await getComponent(moduleName);            new esModule.default(element, container);        })    }}window.onload = function () {    new Main()}// (window as any).Mingle = Main
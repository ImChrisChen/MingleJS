/** * Created by WebStorm. * User: chrischen * Date: 2020/9/15 * Time: 10:54 下午 */// import { App } from './src/app'import './src/app.less'import './src/component/form/select/select.less'// TODO 待解决问题，引入不了其他ts的模块,会找不到模块async function run() {    let elements = document.querySelectorAll('[data-fn]');    elements.forEach(element => {        let moduleName: string = element.getAttribute('data-fn') ?? "";        let names = moduleName.split('-');        // let path = join(__dirname, './src/component', ...names) + '.less'        require('./src/component/form/select/select.less');    })}run().then(r => {});// (window as any).ChrisUI = App;